# =============================================================================
# SmartAttend AI — Backend Dependencies
# PHASE 1: API foundation, auth, database. Computer-vision libraries
# (insightface, mediapipe, onnxruntime, opencv-python-headless) are
# intentionally NOT included yet — they arrive in Phase 2/3 to keep this
# phase's install surface small and easy to validate.
# =============================================================================

# --- Web framework ---
fastapi==0.115.0
uvicorn[standard]==0.30.6

# --- Data validation / settings ---
pydantic==2.9.2
pydantic-settings==2.5.2
email-validator==2.2.0   # required by pydantic's EmailStr (user_model.py)

# --- Reports ---
# Lightweight, pure-Python, no system dependencies — low install risk
# unlike the CV packages above.
fpdf2==2.7.9

# --- Computer vision (Phase 2/3) ---
# NOTE: this is the highest install-risk section of the whole project.
# opencv-python-headless (NOT opencv-python) is required — the regular
# package pulls in GUI bindings that fail on servers/containers without a
# display. insightface needs onnxruntime; if onnxruntime fails to install
# on your Python version, see docs/computer-vision.md for the fallback plan.
numpy==1.26.4
opencv-python-headless==4.10.0.84
onnxruntime==1.19.2
insightface==0.7.3

# --- Database ---
motor==3.6.0
pymongo==4.9.1

# --- Auth ---
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1   # pinned below 4.1: passlib 1.7.4 reads bcrypt.__about__.__version__,
                # which newer bcrypt releases removed — this pin avoids a harmless
                # but noisy AttributeError warning on every hash/verify call.
python-multipart==0.0.9

# --- Env ---
python-dotenv==1.0.1

# =============================================================================
# Dev / test dependencies
# =============================================================================
pytest==8.3.3
pytest-asyncio==0.24.0
httpx==0.27.2
mongomock-motor==0.0.34
